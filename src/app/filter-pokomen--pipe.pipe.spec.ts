import { FilterPokomenPipePipe } from './filter-pokomen--pipe.pipe';

describe('FilterPokomenPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPokomenPipePipe();
    expect(pipe).toBeTruthy();
  });
});
